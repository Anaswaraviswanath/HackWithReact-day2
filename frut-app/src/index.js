import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import FrutApp from './FrutApp';

const root = ReactDOM.createRoot(document.getElementById('frut-app'));
root.render( <
    React.StrictMode >
    <
    FrutApp / >
    <
    /React.StrictMode>
);